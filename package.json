{
  "name": "FilterText",
  "displayName": "Filter Text",
  "description": "Filter selected text through an external shell command.",
  "version": "0.0.1",
  "publisher": "yhirose",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^0.10.1"
  },
  "categories": [
    "Other"
  ],
  "bugs": {
    "url": "https://github.com/yhirose/vscode-filtertext/issues"
  },
  "homepage": "https://github.com/yhirose/vscode-filtertext",
  "repository": {
    "type": "git",
    "url": "https://github.com/yhirose/vscode-filtertext.git"
  },
  "activationEvents": [
    "onCommand:extension.filterText"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.filterText",
        "title": "Filter Text"
      }
    ],
    "keybindings": [
      {
        "key": "ctrl+shift+f",
        "command": "extension.filterText",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
    "compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
  },
  "devDependencies": {
    "typescript": "^1.6.2",
    "vscode": "0.10.x"
  },
  "dependencies": {
    "shell-quote": "^1.4.3",
    "which": "^1.2.0"
  }
}
